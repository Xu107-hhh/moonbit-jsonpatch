Vendored from https://github.com/json-patch/json-patch-tests
(commit on master as of 2026-09-12), files spec_tests.json and tests.json.

Upstream license: Apache-2.0 (declared in the upstream repository's
README.md and package.json). The full license text is included at this
repository's root (LICENSE) — the project itself is Apache-2.0 as well.
These files are test data only and contain no upstream code.

Regenerate the MoonBit tests with: python tools/gen_suite.py
